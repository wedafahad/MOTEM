# متم — نظام تقييم فريق الكتابة (كناية)

نظام تقييم ربعي تفاعلي لفريق كتّاب كناية، مبني وفق [استراتيجية تطوير وتقييم فريق الكتابة](.) وبهوية Kenayah البصرية (الألوان، وخط Ghroob، والشعار).

- **الواجهة**: `web/` — HTML/CSS/JS خالص، بدون أطر عمل أو خطوة بناء (Build)، تُستضاف كملفات ثابتة على GitHub Pages.
- **قاعدة البيانات**: Google Sheets عبر Google Apps Script (`api/Code.gs`) — تعمل من أي جهاز وأي حساب، بدون localStorage وبدون ربط بحساب Claude.
- **خادم اختبار محلي**: `api/mock_server.py` — لتجربة الواجهة على جهازك قبل نشر Apps Script الحقيقي فقط. **لا يُستخدم في الإنتاج.**

---

## 1) نشر قاعدة البيانات (Google Apps Script + Sheets)

1. افتحي [sheets.google.com](https://sheets.google.com) وأنشئي جدول بيانات (Spreadsheet) جديدًا فارغًا، سمّيه مثلًا **"متم — قاعدة البيانات"**.
2. من القائمة: **الإضافات (Extensions) ← Apps Script**.
3. احذفي أي كود موجود في `Code.gs` الافتراضي، والصقي بدلًا منه محتوى الملف [`api/Code.gs`](api/Code.gs) من هذا المستودع بالكامل.
4. احفظي المشروع (Ctrl/Cmd+S)، وسمّيه "متم API".
5. من القائمة العلوية في محرر Apps Script: اختاري الدالة **`setupInitialData`** من القائمة المنسدلة بجانب زر ▶ Run، ثم اضغطي **Run**.
   - أول مرة سيطلب منك Apps Script صلاحيات (Authorize) — وافقي (هذا مشروعك الخاص، لا بيانات ترسَل لأي جهة خارجية).
   - بعد التنفيذ، افتحي **View ← Logs** (أو Ctrl/Cmd+Enter) لرؤية:
     - **كلمة مرور الإدارة العامة**
     - **الأكواد الشخصية** لكل موظف (كاتب/مقيّم) — انسخيها فورًا، لن تظهر هنا مرة أخرى بهذا الشكل (لاحقًا تقدرين تسترجعينها من شاشة «الموظفون» بصلاحية الإدارة داخل التطبيق نفسه).
6. **انشري التطبيق كـ Web App**:
   - **Deploy ← New deployment**.
   - نوع النشر (Select type): **Web app**.
   - Execute as: **Me**.
   - Who has access: **Anyone** (ضروري ليعمل النظام من أي جهاز بدون تسجيل دخول Google).
   - اضغطي **Deploy**، ووافقي على الصلاحيات مرة أخرى إن طُلب.
   - انسخي **رابط Web App URL** الناتج (ينتهي بـ `/exec`) — هذا هو رابط الخادم (`API_URL`) الذي ستُدخلينه في الواجهة.

> **تحديث الكود لاحقًا؟** عدّلي `Code.gs` في المحرر، ثم **Deploy ← Manage deployments ← ✏️ تعديل ← نسخة جديدة (New version) ← Deploy**. رابط `/exec` نفسه يبقى ثابتًا ولا يتغيّر.

### مشاركة نفس القاعدة مع عضو آخر بالفريق
فقط شاركي رابط `/exec` نفسه معه — لا حاجة لأي صلاحيات Google Sheets، فالتطبيق يتحدث مع الرابط مباشرة بمعزل عن صلاحيات الشيت.

---

## 2) نشر الواجهة (GitHub Pages)

### الحالة أ: ليس لديك مستودع بعد
```bash
cd "متم"
git add -A
git commit -m "إطلاق نظام متم"
```
ثم على [github.com/new](https://github.com/new) أنشئي مستودعًا جديدًا (خاص أو عام)، ثم:
```bash
git remote add origin https://github.com/<username>/<repo>.git
git branch -M main
git push -u origin main
```
بعدها من إعدادات المستودع: **Settings ← Pages ← Source: Deploy from a branch ← Branch: main، المجلد: `/web`** ← Save.
سيظهر الرابط الدائم خلال دقيقة على شكل: `https://<username>.github.io/<repo>/`

### الحالة ب: لديك مستودع جاهز
أخبريني باسم المستودع (`owner/repo`) وسأجهّز لك الملفات لدفعها إليه مباشرة، أو أعطيك أوامر `git remote add` + `push` جاهزة لتشغيلها بنفسك.

---

## 3) الربط بين الواجهة والخادم

رابط `/exec` مثبّت مباشرة في `docs/js/config.js` (المتغيّر `API_BASE_URL`) — كل مستخدم يفتح الموقع ويشتغل مباشرة بدون أي شاشة أو إدخال يدوي للرابط.

- **أول مرة فقط**: بعد نشر Apps Script كما في الخطوة (1)، افتحي `docs/js/config.js` وضعي رابط `/exec` قيمة لـ `API_BASE_URL`، ثم ادفعي (`git push`) — بعدها الموقع يعمل لأي زائر مباشرة.
- **عند تحديث الكود لاحقًا**: استخدمي **Deploy ← Manage deployments ← ✏️ تعديل ← نسخة جديدة (New version) ← Deploy** على نفس الـ deployment، وليس "New deployment" — بهذا يبقى رابط `/exec` كما هو ولا تحتاجين تعديل `config.js` مرة أخرى. لو أنشأتِ deployment جديد فعلًا (رابط مختلف)، حدّثي `API_BASE_URL` بالقيمة الجديدة وادفعي.
- سجّلي الدخول بأي كود من الأكواد التي حصلتِ عليها من `Logger`.

---

## 4) الأدوار وبيانات الفريق المبدئية

| الاسم | الدور | يُقيَّم من |
|---|---|---|
| المدير | مقيّم (مدير أعلى) | — |
| وداد الدريني | كاتبة أولى + مقيّمة | المدير |
| ميمونة المرشد | كاتبة | وداد الدريني |
| حمزة السويلم | كاتب | وداد الدريني |
| أفنان الأسمري | كاتبة | وداد الدريني |
| سارة القحطاني | كاتبة | وداد الدريني |
| أحمد بكري | كاتب | وداد الدريني |

- **كاتب**: يسجّل دخوله بكوده الشخصي، يرى لوحته وسجل أعماله فقط، يقيّم نفسه ذاتيًا على مؤشرات التفاعل، ولا يرى تقييمه التفصيلي إلا بعد الاعتماد.
- **مقيّم/مدير مباشر**: يسجّل دخوله بكود منفصل، يرى فريقه المباشر فقط، يقيّمهم، ويرى/يعتمد أيضًا تقييمات الفريق الذي تحت إشراف من يقيّمهم هو (تدقيق على مستويين، تمامًا كآلية التعاير في الاستراتيجية).
- **إدارة عامة**: كلمة مرور منفصلة، تدير الموظفين والتسلسل الهرمي والمعايير والأوزان وسجل التعديلات، لكن **لا** ترى تفاصيل أي تقييم (فقط الحالة والدرجة الإجمالية) — الخصوصية مطبَّقة من الخادم نفسه لا الواجهة فقط.
- **المدير** ("المدير" في الجدول أعلاه — أعلى مقيّم بلا مدير فوقه): يكتسب تلقائيًا نفس صلاحيات الإدارة العامة التشغيلية (الموظفون، المعايير والأوزان، سجل التعديلات) إضافةً لما يملكه كمقيّم من رؤية كاملة لتفاصيل تقييمات كل تسلسله الهرمي — يظهر له ذلك كتبويبات إضافية بعد الدخول بكوده كـ"مقيّم/مدير". الاستثناء الوحيد: تغيير كلمة مرور الإدارة العامة يبقى محصورًا بها هي فقط.

إضافة/حذف موظفين، وتعديل التسلسل الهرمي، كلها من شاشة **«الموظفون»** بصلاحية الإدارة (أو المدير). المعايير والأوزان والحدود الدنيا (Bands) كلها قابلة للتعديل من شاشة **«المعايير والأوزان»**، مع زر **"اقتراح تلقائي"** يقترح حدودًا بناءً على متوسط أداء الفريق الفعلي لكل ربع.

---

## 5) التطوير المحلي (اختياري)

```bash
python3 api/mock_server.py     # خادم اختبار محلي على http://localhost:8787
python3 -m http.server 5500 --directory web   # أو عبر preview_start في Claude Code
```
افتحي `http://localhost:5500`، واختاري "استخدام الخادم المحلي" في شاشة الإعداد. بيانات هذا الخادم تُخزَّن في `api/data/db.json` (مستبعد من Git) — لا علاقة له بالإنتاج.

---

## 6) لوحة العرض

- **«لوحتي»** هي الصفحة الافتراضية لأي جلسة تملك صفة كاتب (كاتب عادي، أو مقيّم يحمل صفة كاتب أيضًا)، ونفس الصفحة تُستخدَم لمراجعة تقييم أي موظف من شاشة «فريقي». تعرض: ملخص سجل الأعمال (عدد الأعمال، المشاريع، أنواع النصوص، نوع الإجراء، نوع الكتابة، عدد الأعمال المشتركة) + القسم الفني + القسم السلوكي (بالمقارنة بين تقييم الكاتب الذاتي وتقييم المقيّم) — مقسَّمة إلى تبويبات لتقليل طول الصفحة. تصدير PDF يطبع كل الأقسام معًا بغضّ النظر عن التبويب المفتوح.
- **«موظف الربع الماضي»** يظهر أعلى شاشة «نظرة عامة» (الإدارة العامة والمدير): ثلاث بطاقات منفصلة (الأعلى فنيًا، الأعلى سلوكيًا، الأعلى كمجموع) من التقييمات المُعتمَدة فقط للربع السابق فعليًا (وليس الربع المختار بجدول الأسفل). حساب "الأعلى فنيًا/سلوكيًا" يحتاج تفاصيل المعايير (`pillarScores`)، وهذه غير متاحة لحساب الإدارة العامة بكلمة المرور (خصوصية التقييم) — فتظهر لها بطاقة "الأعلى كمجموع" فقط مع توضيح السبب؛ المدير يرى الثلاث بطاقات كاملة.

---

## 6.1) إضافات لاحقة — إرسال أعمال الربع، سجل السلوك، وإعادة فتح تقييم معتمَد

- **«إرسال أعمال الربع للاعتماد»**: زر في أعلى «سجل أعمالي» يظهر للكاتبة فقط. يضبط علامة معلوماتية (`workLogStatus`) يراها مقيّمها في «فريقي» وأعلى شاشة «التقييم» — لتعرف أن سجل الأعمال اكتمل ولا حاجة لانتظار عمل إضافي قبل التقييم. أي إضافة/تعديل/حذف لاحق لعمل بنفس الربع يُلغي هذه العلامة تلقائيًا (لا تمنع التعديل، فقط تُنبّه المقيّم أن القائمة تغيّرت عن التي أُرسلت).
- **«سجل السلوك»**: شاشة جديدة (للكاتب على نفسه) وقسم داخل شاشة «التقييم» (للمقيّم على من يقيّمه) لتسجيل وقائع فعلية تدعم ركيزة «التفاعل والمساهمة الجماعية» (عصف ذهني / مشاركة معرفة / مبادرة) — بدل الاعتماد على ذاكرة المقيّم وقت التقييم فقط. البنية كانت موجودة بالخادم من البداية (`upsertBehavioral`/`deleteBehavioral`) لكن بلا أي واجهة تستخدمها.
- **شاشة «التقييم» الآن بتبويبين**: «الأداء الفني» و«المهارات والسلوك» (بدل صفحة طويلة واحدة)، ويظهر أعلاها بادجان: هل أرسلت الكاتبة أعمال الربع، وهل اعتمدت تقييمها الذاتي — مع شريط درجة الكاتب الذاتية أعلى كل معيار سلوكي فور إرسالها له (مقارنة مباشرة مع درجة المقيّم، لا نصًا صغيرًا فقط).
- **قفل التقييم المعتمَد فعليًا**: كانت شارة «مُعتمَد — لا يمكن التعديل إلا عبر إعادة الفتح من الإدارة» نصًا فقط بلا أي حماية أو آلية فتح فعلية — أي مقيّم كان يقدر يعدّل ويحفظ فوق تقييم معتمَد وصل فعليًا للكاتبة بصمت. الآن: كل حقول الإدخال تُعطَّل فعليًا فور الاعتماد، ويظهر بدلاً من أزرار الحفظ زر «إعادة فتح التقييم» — يعيده لحالة «مُرسَل» (بنفس صلاحية الاعتماد بالضبط) للتعديل ثم الاعتماد من جديد.
- **إعادة الفتح صارت متاحة أيضًا من شاشة «مراجعة»**: زر إعادة الفتح كان موجودًا فقط داخل شاشة «التقييم» المباشرة، وهذه لا تُفتح إلا لتقاريرك المباشرين. مدير المقيّم (يشرف هرميًا دون أن يكون المدير المباشر لمن اعتُمد تقييمه) كان يصل لتقييمات مرؤوسي مرؤوسيه فقط عبر شاشة «مراجعة» من «فريقي»، ولم يكن فيها زر إعادة فتح إطلاقًا — فبمجرد الاعتماد من هذا المسار، لا وسيلة بالواجهة لإعادة فتحه رغم أن الصلاحية موجودة أصلًا بالخادم. أُضيف الزر لنفس شاشة «مراجعة» حين يكون التقييم معتمَدًا.
- **فرض مجموع أوزان الركائز = 100%**: الدرجة الكلية تُحسب دائمًا كنسبة من 100 — إن اختلّ مجموع أوزان الكاتب أو الكاتب الأول (خطأ عند تعديل الأوزان في شاشة «المعايير والأوزان») كانت كل درجات الكتّاب تُحتسب خطأً رياضيًا بصمت، مع تحذير نصي فقط لا يمنع الحفظ. الحفظ الآن مرفوض (في الواجهة وفي الخادم معًا) ما لم يساوِ كل مجموع 100% تمامًا، ورقم المجموع يظهر بالأحمر حين يختل.

---

## 6.2) إضافات لاحقة — سجل السلوك الكامل، تسبيب الدرجات، مقترحات المقيّم، وإلغاء الاعتماد تلقائيًا

- **«سجل السلوك» أصبح يظهر كاملًا للمقيّم/المدير**: كان مقصورًا على قسم داخل شاشة «التقييم» لموظف واحد فقط وقت تقييمه. الآن أي مقيّم يرى شاشة «سجل السلوك» في القائمة الجانبية تعرض سجل فريقه المباشر (باختيار العضو)، والمدير (أعلى الهرم) يرى إضافة لذلك شاشة «سجل سلوك الموظفين» بكل الكتّاب — بنفس بنية «سجل أعمال الموظفين» تمامًا.
- **تسبيب الدرجة عند أقل من 3**: في شاشة التقييم، أي معيار (فني أو سلوكي، بما فيها معايير عيّنات الجودة لكل عمل) يُعطى درجة أقل من 3 يفتح تلقائيًا أسفله مربع نص لسبب الدرجة — إلزامي قبل الإرسال للاعتماد. السبب يظهر لاحقًا للكاتبة والمدير في لوحتها/شاشة المراجعة.
- **«ملاحظات المقيّم» → «مقترحات المقيّم»**: انتقلت لأعلى شاشة التقييم (قبل تبويبَي الفني/السلوكي بدل أسفلهما)، وتغيّر اسمها، وتظهر الآن في تبويب «ملخص الأعمال» بلوحة الكاتبة فور اعتماد التقييم (لا فقط تبويب السلوك)، وأُضيفت كعمود في ملف تصدير Excel.
- **إلغاء الاعتماد تلقائيًا عند تغيّر سجل الأعمال**: إضافة/تعديل/حذف عمل لنفس الربع بعد أن أُرسل تقييمه للاعتماد أو اعتُمد فعليًا كان يترك حالة التقييم على حالها رغم أن القائمة التي بُني عليها التقييم تغيّرت — الآن يعيد التقييم تلقائيًا لحالة «مسودة» (ويمسح الاعتماد) ليُعاد تقييمه وإرساله واعتماده من جديد على القائمة الجديدة.
- **خيار «أخرى» في سجل السلوك والمستندات**: قائمة مؤشر الواقعة السلوكية وقائمة نوع المستند أصبح فيهما خيار «أخرى» مع حقل نص لتحديد النوع يدويًا — بنفس مبدأ «أخرى» في سجل الأعمال.

## 6.3) إضافة أخيرة — مقيّم بنطاق موسّع (مثال: مدير إبداعي بلا تسلسل هرمي)

- من شاشة «الموظفون»، أي مقيّم جديد يظهر له الآن حقل **«نطاق تقييمه/إشرافه»** مع ثلاثة خيارات: التسلسل الهرمي فقط (الافتراضي القديم)، أو كتّاب إضافيون محدَّدون فوق تسلسله، أو **كل الكتّاب بمعزل عن التسلسل الهرمي بالكامل**.
- لإنشاء دور مثل «المدير الإبداعي» الذي لا يقيّمه أحد ويقيّم/يطّلع على الجميع: أضيفيه من «الموظفون» بلا «مدير مباشر» (يجعله أيضًا يكتسب صلاحيات المدير التشغيلية تلقائيًا — نفس قاعدة "المدير" الحالية)، واختاري له نطاق «كل الكتّاب».
- هذا النطاق **إضافي** فوق التسلسل الهرمي العادي، لا بديل عنه — يمنح تقييمًا/اطّلاعًا كاملًا (أعمال، سلوك، مستندات، تقييمات) على من في النطاق، ويظهر في «فريقي»/«التقييم»/«سجل الأعمال»/«سجل السلوك»/«مستندات الفريق» تلقائيًا بلا حاجة لأي تعديل إضافي.
- تنبيه: التقييم في هذا النظام صفٌّ واحد لكل موظف/ربع — إن قيّم «المدير الإبداعي» كاتبةً لها مقيّم أصلي بالفعل هذا الربع، تقييمه يحلّ محل تقييم مقيّمها الأصلي لنفس الربع (لا تقييمان متوازيان).

## 7) الأرشفة الفصلية

بعد 21 يومًا من بداية كل ربع ميلادي جديد، يُنقَل سجل الربع السابق (WorkLog/BehavioralLog/EvalScores) إلى أوراق أرشيف منفصلة (نقل لا حذف) بدل بقائه في الأوراق الحيّة — التفاصيل الكاملة والآلية في `api/contract.md`. **هذا لا يُفعَّل تلقائيًا** — التفعيل خطوة يدوية: شغّلي `installQuarterlyArchiveTrigger` مرة واحدة من محرر Apps Script بعد مراجعة الآلية (تمامًا كخطوة `setupInitialData`).

---

## 8) قيود معروفة وأفكار للتوسعة لاحقًا

- **الأعمال التشاركية**: البنية تدعم مبدئيًا حقل `collaborators` بنسبة توزيع، لكن لا توجد بعد واجهة لإضافة أكثر من كاتب على نفس العمل — التوزيع يفترض حاليًا 100% لصاحب العمل. يمكن إضافتها لاحقًا في نموذج «إضافة عمل».

هذه النقطة لا تمنع الاستخدام الفعلي للنظام اليوم، لكنها تحتاج جولة تطوير إضافية إن أردتِ تفعيلها بالكامل.
